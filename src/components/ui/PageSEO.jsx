/**
 * SEO on the Next.js build is handled server-side through the Metadata API
 * (see each `app/**\/page.js` and `app/layout.js`), so this component is a
 * no-op kept for source parity with the Vite app.
 */
export default function PageSEO() {
  return null;
}
